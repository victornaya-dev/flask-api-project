apiVersion: v1
kind: Service
metadata:
  name: flask-api-service
  labels:
    app: flask-api
spec:
  selector:
    app: flask-api
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 80
      nodePort: 30008
  type: NodePort